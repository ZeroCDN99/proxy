:{$PORT:80} {
    # 1. Запрет прямого доступа к боту и базе данных[span_0](start_span)[span_0](end_span)[span_1](start_span)[span_1](end_span)
    @blocked path /bot.php* /db.php*
    respond @blocked 403

    # 2. Отсечение ботов и сканеров без токена (сразу 200 OK без нагрузки на Worker)
    @notoken {
        not query token=*
    }
    respond @notoken "OK" 200

    # 3. Проксирование через Cloudflare Worker на Alwaysdata[span_2](start_span)[span_2](end_span)[span_3](start_span)[span_3](end_span)
    reverse_proxy https://dawn-credit-4072.lonerlonely78.workers.dev {
        header_up Host dawn-credit-4072.lonerlonely78.workers.dev
        header_up X-Fwd-Ip {remote_host}
        header_up X-Fwd-Secret "968f6dacaf5292b98bef45591d73e0402a13cb8884c8bb7a"
        header_up X-Fwd-Proto "https"
        header_up X-Fwd-Host "cozy-market.zeabur.app"
        header_down X-Mirror "cozy-market.zeabur.app"

        transport http {
            tls
            dial_timeout 10s
        }
    }
}
